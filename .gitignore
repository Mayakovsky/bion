# Secrets / local env — never commit
.env
.env.local
.env.test
*.local

# Node
node_modules/
dist/
*.tsbuildinfo
pnpm-debug.log*

# Bion runtime state (daemon heartbeat, mailbox, usage) — never committed
.bion/

# Local Postgres cluster lives OUTSIDE the repo (~/.bion-pg); never vendored here.
pgdata/
*.superpw
*.rwpw
*.ownerpw

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
